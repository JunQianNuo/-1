# 问题二实际提交代码：修复与运行说明

## 一、复制文件

将：

```text
apply_q2_actual_fix.py
verify_q2_actual_fix.py
```

复制到：

```text
D:\知识库\数学建模\第一次\代码\问题二
```

与 `run_q2_optimized_search.py`、`q2_kdtree_coverage.py` 放在同一目录。

## 二、应用补丁

```bat
cd /d D:\知识库\数学建模\第一次\代码\问题二
python apply_q2_actual_fix.py
```

程序会自动备份原文件：

```text
run_q2_optimized_search.py.bak_before_actual_fix
q2_kdtree_coverage.py.bak_before_actual_fix
```

成功时应显示：

```text
SUCCESS: actual submitted code patched and compiled.
```

## 三、验证源码

```bat
python verify_q2_actual_fix.py --source-only
```

应显示：

```text
PASS: source patch and requested output checks succeeded.
```

## 四、小规模验收运行

不要立刻跑正式全量。先执行：

```bat
python run_q2_optimized_search.py ^
  --start-total 40 ^
  --stop-total 40 ^
  --samples-per-structure 1 ^
  --local-starts-per-structure 1 ^
  --keep-top-per-total 2 ^
  --max-cegis-rounds 1 ^
  --local-max-evaluations 30 ^
  --output-dir results_actual_fix_smoke
```

修复后每个初筛候选会完整检查73个时刻，且活动集覆盖24小时，因此会比旧版明显更慢。

## 五、验证输出

```bat
python verify_q2_actual_fix.py results_actual_fix_smoke
```

必须满足：

```text
screen evaluated_time_steps = 73
screen stopped_early = False
screen max_uncovered_gap_s <= 21600
refined evaluated_time_steps = 721
refined max_uncovered_gap_s <= 86400
sum(audit.selected_for_refinement) = refined_record_count
```

并显示 `PASS`。
